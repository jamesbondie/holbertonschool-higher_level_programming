#!/usr/bin/python3

def add_tuple(tuple_a=(), tuple_b=()):
    new_tuple = ''
    i = 0
    if (len(tuple_a) > len(tuple_b):
        for i in tuple_b:
               new_tuple = ([tuple_a[i] + tuple_b[i]
